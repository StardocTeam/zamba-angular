@model IEnumerable<Zamba.WebAdmin.Models.USERADMIN>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<a href="@Url.Action("Create", "USERADMINs")" class="btn btn-primary btn-md " style="margin-bottom:10px;margin-top:10px">
    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> crear nuevo
</a>

<table class="table">
    <tr><th>
    <label>Opciones </label>
</th>
        <th>
            @Html.DisplayNameFor(model => model.idadmin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
      
    </tr>

@foreach (var item in Model) {
    <tr>
        <td style="display:table-cell">
            <a href="@Url.Action("Edit", "USERADMINs", new { id = item.id })" class="btn btn-primary btn-xs" style="margin-right:2px;">
                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            </a>
            @*@Html.ActionLink("Editar", "Edit", new { id = item.id }) |*@
            <a href="@Url.Action("Details", "USERADMINs", new { id = item.id })" class="btn btn-primary  btn-xs" style="margin-right:2px;">
                <span class="glyphicon glyphicon-zoom-in" aria-hidden="true"></span>
            </a>
            @*@Html.ActionLink("Detalles", "Details", new { id = item.id }) |*@

            <a href="@Url.Action("Delete", "USERADMINs", new { id = item.id })" class="btn btn-primary  btn-xs" style="margin-right:2px;">
                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
            </a>
            @*@Html.ActionLink("Borrar", "Delete", new { id = item.id })*@
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idadmin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>

        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>*@
    </tr>
}

</table>
