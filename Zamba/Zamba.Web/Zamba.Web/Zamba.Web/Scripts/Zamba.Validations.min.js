function ExtendValidator() {
    jQuery.validator.addMethod("greaterThan", function (value, element, params) {
        var element = document.getElementById(params); var comparateValue = (element) ? element.value : params; if (value == "")
            return !0; if (!/Invalid|NaN/.test(new Date(value))) { return new parseDate(value) < parseDate(comparateValue) }
        return isNaN(value) && isNaN($(params).val()) || (Number(value) < Number(comparateValue))
    }, 'Debe ser mayor a {0}.'); jQuery.validator.addMethod("lessThan", function (value, element, params) {
        var element = document.getElementById(params); var comparateValue = (element) ? element.value : params; if (value == "")
            return !0; if (!/Invalid|NaN/.test(new Date(value))) { return parseDate(value) > parseDate(comparateValue) }
        return isNaN(value) && isNaN($(params).val()) || (Number(value) > Number(comparateValue))
    }, 'Debe ser menor a {0}.'); jQuery.validator.addMethod("greaterEqualThan", function (value, element, params) {
        var element = document.getElementById(params); var comparateValue = (element) ? element.value : params; if (value == "")
            return !0; if (!/Invalid|NaN/.test(new Date(value))) { return new parseDate(value) <= parseDate(comparateValue) }
        return isNaN(value) && isNaN($(params).val()) || (Number(value) <= Number(comparateValue))
    }, 'Debe ser mayor o igual a {0}.'); jQuery.validator.addMethod("lessEqualThan", function (value, element, params) {
        var element = document.getElementById(params); var comparateValue = (element) ? element.value : params; if (value == "")
            return !0; if (!/Invalid|NaN/.test(new Date(value))) { return new parseDate(value) >= parseDate(comparateValue) }
        return isNaN(value) && isNaN($(params).val()) || (Number(value) >= Number(comparateValue))
    }, 'Debe ser menor o igual a {0}.'); jQuery.validator.addMethod("dateAR", function (value, element, params) {
        if (params && value != "" && value != null && element) {
            if (parseDate(value)) { return !0 }
            else { return !1 }
        }
        else return !0
    }, 'Debe introducir una fecha valida.'); return !0
}